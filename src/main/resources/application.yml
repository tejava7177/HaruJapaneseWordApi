server:
  port: 8080

spring:
  application:
    name: haru-api

  datasource:
    url: jdbc:mysql://localhost:3306/haru
    username: root
    password: root1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
